# Changelog

All notable changes to the shared standards live here so you can tell, at a
glance, whether it's worth running `-Update` in a given repo.

## [Unreleased]

- Initial scaffold: .editorconfig, PSScriptAnalyzer settings, ruff config,
  Directory.Build.props, ESLint flat config + Prettier, pre-commit config,
  and GitHub Actions lint-only templates for PowerShell/Python/.NET/Node.
